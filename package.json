{
  "name": "kairos-monorepo",
  "private": true,
  "scripts": {
    "postinstall": "cd kairosBack && npm install && cd ../kairosFront && npm install",
    "build": "cd kairosBack && npm run build && cd ../kairosFront && npm run build",
    "start": "cd kairosBack && npm run db:migrate && node dist/main"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
